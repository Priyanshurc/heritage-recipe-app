# =============================================================================
# Heritage Recipe App - Backend Configuration
# =============================================================================
# DO NOT COMMIT THIS FILE TO GIT (it's in .gitignore)
# Copy this file to .env and fill in your real values
# =============================================================================

# DATABASE CONFIGURATION
# =====================
# Choose ONE of the options below:
#
# Option 1: MongoDB Atlas (Cloud, FREE tier - RECOMMENDED for first-time setup)
# - Sign up at: https://www.mongodb.com/cloud/atlas
# - Create a free cluster
# - Get connection string from: Cluster > Connect > Drivers > Node.js
# - Format: mongodb+srv://username:password@cluster.mongodb.net/database_name
# MONGODB_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@cluster0.mongodb.net/test?retryWrites=true&w=majority
#
# Option 2: Local MongoDB (must be running on your machine)
# - Linux: sudo apt install mongodb-org && sudo systemctl start mongod
# - macOS: brew install mongodb-community && brew services start mongodb-community
# - Windows: Download from https://www.mongodb.com/try/download/community
# MONGODB_URI=mongodb://localhost:27017/heritage-recipe-db
#
# Option 3: Docker MongoDB (if Docker is installed)
# - Run: docker-compose up -d
# MONGODB_URI=mongodb://admin:password123@localhost:27017/heritage-recipe-db?authSource=admin

MONGODB_URI=mongodb://localhost:27017/heritage-recipe-db

# SERVER CONFIGURATION
# ====================
# Port on which the backend will run
PORT=5000

# JWT AUTHENTICATION
# ==================
# Use a long random string (20+ characters) for security
# Generate one: openssl rand -hex 32  (Linux/macOS)
# Or visit: https://generate-random.org/
JWT_SECRET=your_super_secret_jwt_key_here_change_me_to_random_string

# Optional: Token expiration time
JWT_EXPIRE=7d

# FRONTEND URL (for CORS)
# =======================
# If frontend is running on a different origin, set it here
# For local development (frontend on same machine):
FRONTEND_URL=http://localhost:8000

# =============================================================================
# SETUP HELP
# =============================================================================
# After filling this file:
# 1. Run: npm run dev (or npm start)
# 2. You should see "MongoDB connected successfully"
# 3. Frontend should be able to call http://localhost:5000/api
#
# Troubleshooting:
# - No MongoDB? Use Docker: docker-compose up -d
# - Port 5000 busy? Change PORT to 3001, also update frontend baseUrl
# - Wrong connection string? Check backend logs for details
# =============================================================================